<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ds130626.pmu.rti.etf.rs.accelgame">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".android.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".android.game.GameActivity"
            android:screenOrientation="portrait"
            android:theme="@style/GameTheme" />
        <activity
            android:name=".android.build.LevelBuilderActivity"
            android:screenOrientation="portrait"
            android:theme="@style/GameTheme" />
        <activity android:name=".android.score.ScoresActivity" />
        <activity
            android:name=".android.settings.SettingsActivity"
            android:label="@string/title_activity_settings" />
    </application>

</manifest>